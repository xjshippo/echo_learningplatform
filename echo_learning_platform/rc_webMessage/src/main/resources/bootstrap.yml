spring:
  application:
    name: rc-webMessage
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      config:
        file-extension: yaml # 文件后缀名
      discovery:
        server-addr:  localhost:8848 # nacos地址

# 配置jwt
jwt:
  # 加密密钥
  secret: b96bb6cccf5df779edd6e141a0bc6d52
  # token有效时长
  expire: 3600
  # header 名称
  header: token