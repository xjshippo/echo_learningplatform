spring:
  application:
    name: rc-comment
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      config:
        file-extension: yaml # 文件后缀名
      discovery:
        server-addr:  localhost:8848 # nacos地址